---
import '../styles/global.css';
import '../styles/Cryptopage.css';
import '../styles/pruebas.css';
import '../styles/pruebas2.css';

const { pageTitle } = Astro.props;
const blogs = await Astro.glob('../pages/tecnologia/*.md');

const sortedFiles = blogs.sort((a, b) => {
  const bDate = new Date(a.frontmatter.pubDate);
  const aDate = new Date(b.frontmatter.pubDate);

  return aDate - bDate;
});




---

<html lang="es">
	<head>
    <meta name="robots" content="noindex">
    <meta name="robots" content="nofollow">
        <!-- Google tag (gtag.js) -->
<!--<script type="text/partytown" async src="https://www.googletagmanager.com/gtag/js?id=MEASUREMENT_ID"></script>
<script type="text/partytown">
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-E2NHX0J9W9');
</script> -->
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
		
		<style>
            @import url('https://fonts.cdnfonts.com/css/helvetica-255');
            </style>
		<title>Pruebas| Plankton News</title>
        <link rel="icon" type="image/x-icon" href="https://raw.githubusercontent.com/AntonioHerrera1994/news-astro/master/favpw.ico">
        
        <slot />
	</head>


	<body>
      <!--  ******* descripciones cortas*******
    <section class="blog-section" id="blog-section">
      <div class="grid-cont">
        <ul class="blogs">
          {blogs.map((blog) => (
            <li>
              <article class="blog">
                <img src={blog.frontmatter.image.url} alt="blog.frontmatter.image.alt" width="100%" />
                <a href={blog.url} class="">
                  <h3>
                    {/* Truncate title using substring */}
                    {blog.frontmatter.title.substring(0, 90) + (blog.frontmatter.title.length > 50 ? "..." : "")}
                  </h3>
                </a>
                <p>
                  {/* Truncate description using substring */}
                  {blog.frontmatter.description.substring(0, 160) + (blog.frontmatter.description.length > 50 ? "..." : "")}
                </p>
              </article>
            </li>
          ))}
        </ul>
      </div>
    </section> -->


    <section class="blog-section" id="blog-section">
      <div class="grid-cont">
      
        <ul class="blogs">
          {blogs.map((blog) =>
            <li>
              <article class="blog">          
                <img src={blog.frontmatter.image.url} alt="blog.frontmatter.image.alt" width="100%"/>
                <a href={blog.url} class=""><h3>{blog.frontmatter.title}</h3></a>
                <p>{blog.frontmatter.description}</p>

              </article>            
            </li>
          )}
        </ul>
      </div>
    </section>


    
    

<!-- Primer codigo de map -->
<!--  <section class="blog-section" id="blog-section">
  <div class="grid-cont">
    <ul class="blogs">
      {blogs.slice(0, 4).map((blog) => (
        <li>
          <article class="blog">
            <img src={blog.frontmatter.image.url} alt="blog.frontmatter.image.alt" width="100%" />
            <a href={blog.url} class=""><h3>{blog.frontmatter.title}</h3></a>
            <p>{blog.frontmatter.description}</p>
          </article>
        </li>
      ))}
    </ul>
  </div>
</section> -->

<section class="blog-section2" id="blog-section2">
  <div class="grid-cont2">

    <!-- Parte izquierda del grid -->
    <div class="izq-grid2"> 

    <ul class="blogs1">
      {blogs.slice(0, 1).map((blog) => (
        <li>
          <article class="blog">

            <a href={blog.url} class=""><h3>{blog.frontmatter.title}</h3></a>
            <p>{blog.frontmatter.description}</p>
            <h4 >{blog.frontmatter.author} | {blog.frontmatter.date} | {blog.frontmatter.category}</h4>
          </article>
        </li>
      ))}
    </ul>

    <div class="container">
    <ul class="blogs2">
      {blogs.slice(0, 1).map((blog) => (
        <li>
          <article class="blog-image">
       
            <img src={blog.frontmatter.image.url} alt="blog.frontmatter.image.alt" class="img-central"/>
            
          </article>
        </li>
      ))}
    </ul>
    </div>

  </div>

   <!--Fin  Parte izquierda del grid -->

    <ul class="blogs3">
      {blogs.slice(1, 5).map((blog) => (
        <li>
          <article class="blog-grid-right">
            <div>
            
            <img src={blog.frontmatter.image.url} alt="blog.frontmatter.image.alt" width="100%" />
          </div>

          <div class="meta-grid-3">
            <h4 >{blog.frontmatter.author} | {blog.frontmatter.date}</h4>
            <a href={blog.url} class=""><h3>{blog.frontmatter.title}</h3></a>
           <h4>{blog.frontmatter.category}</h4>
          </div>
          </article>
        </li>
      ))}
    </ul>


  </div>
</section>

    </body>

    
    </html>
		

